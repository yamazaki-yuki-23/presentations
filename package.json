{
  "name": "presentations",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "marp -s",
    "build:pdf": "marp -o dist/slides.pdf",
    "build:html": "marp -o dist/slides.html",
    "build:pptx": "marp -o dist/slides.pptx"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.2.3"
  }
}
